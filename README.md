# E-Commerce Sales & Customer Analytics Platform

An end-to-end analytics pipeline: synthetic e-commerce data → PostgreSQL
(normalized schema + materialized views) → Python ETL (extract/transform/load)
→ Power BI dashboard. Built to mirror how this kind of internal analytics
tool actually gets built at a company.

